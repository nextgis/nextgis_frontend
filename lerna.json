{
  "packages": [
    "packages/*"
  ],
  "npmClient": "yarn",
  "version": "1.19.5",
  "command": {
    "version": {
      "message": "chore(release): publish %s"
    },
    "run": {
      "ignore": [
        "@nextgis/demo-app",
        "@nextgis/build-tools",
        "@nextgis/eslint-config"
      ]
    },
    "publish": {
      "ignoreChanges": [
        "@nextgis/frontend_test",
        "@nextgis/demo-app"
      ]
    }
  }
}
