{
  "name": "@nextgis-example/demo-webmap-layer-tree",
  "private": true,
  "displayName": "Webmap layers tree",
  "description": "Create a tree control to show and hide layers from the webmap. Click on the checkboxes to toggle the visibility of layers and groups. Holding the CTRL key while clicking on a group checkbox propagates the action to all nested layers. Use the sliders to adjust opacity, which is applied hierarchically and summed with group opacity values.",
  "tags": [
    "tree control",
    "layers",
    "visibility",
    "opacity",
    "groups"
  ],
  "ngwMaps": [
    "ngw-leaflet",
    "ngw-ol",
    "ngw-maplibre-gl"
  ],
  "scripts": {
    "start": "yarn run start:leaflet",
    "start:leaflet": "yarn run -T example --adapter=ngw-leaflet",
    "start:ol": "yarn run -T example --adapter=ngw-ol",
    "start:maplibre-gl": "yarn run -T example --adapter=ngw-maplibre-gl"
  },
  "dependencies": {
    "@nextgis/ngw-kit": "*",
    "@nextgis/ngw-leaflet": "*"
  },
  "devDependencies": {
    "@nextgis/ngw-maplibre-gl": "*",
    "@nextgis/ngw-ol": "*",
    "vite": "^6.3.5"
  }
}
