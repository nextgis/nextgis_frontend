{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.5.1",
    "rimraf": "^2.6.2",
    "topological-sort": "^0.3.0",
    "tslint": "^5.11.0",
    "tslint-webpack-plugin": "^1.3.0",
    "webpack": "^4.x.x"
  },
  "scripts": {
    "clean": "lerna run clean",
    "bootstrap": "lerna run bootstrap",
    "dev": "lerna run dev",
    "build": "lerna run build",
    "prod": "node ./webpack.config.js",
    "demo": "cd ./packages/demo/ && yarn run prod",
    "test": "echo \"Error: run tests from root\" && exit 1"
  },
  "workspaces": [
    "packages/*"
  ]
}
