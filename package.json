{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "scripts": {
    "clean": "lerna run clean",
    "bootstrap": "lerna run bootstrap",
    "dev": "lerna run dev",
    "prod": "cd ./packages/webmap/ && yarn run prod && lerna run build",
    "build": "lerna run build",
    "demo": "cd ./packages/demo/ && yarn run prod",
    "test": "echo \"Error: run tests from root\" && exit 1"
  },
  "workspaces": [
    "packages/*"
  ]
}
