{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.11.1",
    "rimraf": "^2.6.3",
    "topological-sort": "^0.3.0",
    "tslint": "^5.12.1",
    "tslint-webpack-plugin": "^2.0.2",
    "webpack": "^4.29.3"
  },
  "scripts": {
    "clean": "lerna run clean",
    "bootstrap": "lerna run bootstrap",
    "dev": "lerna run dev",
    "build": "lerna run build",
    "prod": "node ./webpack.config.js",
    "demo": "cd ./packages/demo/ && yarn run prod",
    "test": "echo \"Error: run tests from root\" && exit 1",
    "doc": "typedoc --json ./packages/demo/src/api.json --exclude **/packages/demo/**/* ./packages"
  },
  "license": "LGPL-3.0",
  "workspaces": [
    "packages/*"
  ]
}
