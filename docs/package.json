{
  "name": "@nextgis/frontend_docs",
  "private": true,
  "type": "module",
  "version": "3.0.0",
  "scripts": {
    "doc": "yarn build",
    "build": "yarn build:api && yarn build:llms",
    "build:api": "node ./node_modules/typedoc/bin/typedoc",
    "build:llms": "node ./scripts/buildLlms.mjs",
    "docker": "dockpkg -r harbor.nextgis.net -n frontend/code-api -l -d ./docker"
  },
  "devDependencies": {
    "docmypkg": "^0.0.1",
    "typedoc": "^0.28.20",
    "typedoc-plugin-markdown": "^4.13.0"
  },
  "license": "MIT"
}
