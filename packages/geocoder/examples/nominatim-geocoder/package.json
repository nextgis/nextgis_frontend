{
  "name": "@nextgis-example/geocoder-nominatim-geocoder",
  "private": true,
  "displayName": "Geocoding using Nominatim",
  "description": "Perform geocoding using Nominatim API to retrieve location information and show a list of unique values based on the specified address fields.",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8080 --strictPort --open",
    "build": "vite build --base=./"
  },
  "dependencies": {
    "@nextgis/geocoder": "workspace:*",
    "@nextgis/ngw-kit": "workspace:*",
    "@nextgis/ngw-leaflet": "workspace:*",
    "@nextgis/utils": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
