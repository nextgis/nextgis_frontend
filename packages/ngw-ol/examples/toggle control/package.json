{
  "name": "@nextgis-example/ngw-ol-toggle-control-legacy",
  "private": true,
  "displayName": "Toggle button control",
  "description": "This is a two-state control button. Useful for switching certain modes of operation with the card.",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8080 --strictPort --open",
    "build": "vite build --base=./"
  },
  "dependencies": {
    "@nextgis/ngw-ol": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
