{
  "name": "@nextgis-example/ngw-connector-cancelable",
  "private": true,
  "displayName": "Cancelable Request Example",
  "description": "Demonstrates how to create and abort a cancellable API request using the NgwConnector and AbortController.",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8080 --strictPort --open",
    "build": "vite build --base=./"
  },
  "dependencies": {
    "@nextgis/ngw-connector": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
