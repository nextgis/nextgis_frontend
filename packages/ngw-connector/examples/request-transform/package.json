{
  "name": "@nextgis-example/ngw-connector-request-transform",
  "private": true,
  "displayName": "Request transform",
  "description": "The ability to change each request at any time before sending",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8080 --strictPort --open",
    "build": "vite build --base=./"
  },
  "dependencies": {
    "@nextgis/ngw-connector": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
