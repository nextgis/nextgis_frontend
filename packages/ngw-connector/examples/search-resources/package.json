{
  "name": "@nextgis-example/ngw-connector-search-resources",
  "private": true,
  "displayName": "Search resources",
  "description": "Use search API to get list of resources you need",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8080 --strictPort --open",
    "build": "vite build --base=./"
  },
  "dependencies": {
    "@nextgis/ngw-connector": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
