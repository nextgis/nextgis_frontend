{
  "name": "@nextgis-example/qms-leaflet-qms-services",
  "private": true,
  "displayName": "QMS services with Leaflet",
  "description": "Add TMS and WMS services from NextGIS QMS to a Leaflet map",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8080 --strictPort --open",
    "build": "vite build --base=./"
  },
  "dependencies": {
    "@nextgis/qms-leaflet": "workspace:*",
    "leaflet": "1.7.1"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
