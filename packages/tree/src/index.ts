import { treeEvery, treeFilter, treeFind, treeSome } from './treeFilter';

export { treeEvery, treeFilter, treeFind, treeSome };

// shortcut to use like `Tree.find`
export {
  treeEvery as every,
  treeFilter as filter,
  treeFind as find,
  treeSome as some,
};
