{
  "name": "@nextgis-example/ngw-uploader-input-simple_input",
  "private": true,
  "displayName": "Raster upload with createInput",
  "description": "A simple example showing how to use ngw-uploader-input to upload a GEOTIFF to the NextGIS Web cloud",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8080 --strictPort --open",
    "build": "vite build --base=./"
  },
  "dependencies": {
    "@nextgis/ngw-uploader-input": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
