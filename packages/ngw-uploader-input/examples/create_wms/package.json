{
  "name": "@nextgis-example/ngw-uploader-input-create_wms",
  "private": true,
  "displayName": "Create WMS service",
  "description": "Use of additional `createInput`-method parameters for processing the result, error and name formation. Demonstration of creating WMS service from style",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8080 --strictPort --open",
    "build": "vite build --base=./"
  },
  "dependencies": {
    "@nextgis/ngw-uploader-input": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
