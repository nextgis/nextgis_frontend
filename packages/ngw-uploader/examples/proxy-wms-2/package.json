{
  "name": "@nextgis-example/ngw-uploader-proxy-wms-2",
  "private": true,
  "displayName": "Vendor param in WMS Layer",
  "description": "Create proxy WMS service from remote WMS layers with vendor parameters.",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8080 --strictPort --open",
    "build": "vite build --base=./"
  },
  "dependencies": {
    "@nextgis/ngw-uploader": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
