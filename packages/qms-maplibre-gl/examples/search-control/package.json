{
  "name": "@nextgis-example/qms-maplibre-gl-search-control",
  "private": true,
  "displayName": "QMS control with MapLibre GL",
  "description": "Search the QMS API or browse the catalog and add a service",
  "scripts": {
    "start": "yarn run -T example",
    "build": "yarn run -T example:build"
  },
  "dependencies": {
    "@nextgis/qms-maplibre-gl": "workspace:*",
    "maplibre-gl": "^6.5.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
